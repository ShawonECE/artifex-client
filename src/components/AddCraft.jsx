import { Helmet } from "react-helmet-async";

const AddCraft = () => {
    return (
        <div>
            <Helmet>
                <title>Artifex | Add Craft</title>
            </Helmet>
        </div>
    );
};

export default AddCraft;